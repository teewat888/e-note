<% @notes.each do |note| %>

<div class="card">
    <div class="card-header">
        <h5 class="mb-0"><%= note.title %> (by <%= note.user.username %>)</h5>
    </div>
    <div class="card-body">
        <p class="card-text"><%= note.content %></p>
    </div>
</div>
   
<% end %>